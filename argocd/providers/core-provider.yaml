# Provider "core" : composants Cluster API de base (v1.9.x).
# Adapter la version si une version differente est deja validee.
apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: CoreProvider
metadata:
  name: cluster-api
  namespace: capi-operator-system
spec:
  version: v1.9.5
